#ifndef WHATEVER_HPP
#define WHATEVER_HPP

#include <ostream>
#include <iostream>
#include <string>
#include <cstring>
#include <iomanip>

template <typename T>
T max(T& a, T& b);

template <typename T>
T min(T& a, T& b);

#endif